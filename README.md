# Django App Terraform Module (Dockerized)

Manage a "standardized" Django application's Web and Celery components.

## Quick Facts

* Parameters are passed as environment variables stored into a .env file under the project's directory.

## TODO

Set media & static directory's ownership to app user.
